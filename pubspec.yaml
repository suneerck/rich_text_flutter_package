name: rich_text_editor
description: A cross-platform Flutter rich text editor with HTML input and output. Supports web, iOS, Android, macOS, and Windows.
version: 1.0.0
homepage: https://github.com/yourusername/rich_text_editor
repository: https://github.com/yourusername/rich_text_editor
issue_tracker: https://github.com/yourusername/rich_text_editor/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
